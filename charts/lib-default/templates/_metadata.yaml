{{- define "library.metadata" }}
metadata:
  name: {{ include "library.factory.resource_name" . }}
  {{- if ne (toString .Values.scope) "cluster" }}
  namespace: {{ coalesce .Values.resource_namespace .Release.Namespace }}
  {{- end }}
  {{- if .Values.annotations }}
  annotations: {{- include "library.factory.kv_list" .Values.annotations | indent 4 }}
  {{- end }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    {{- include "library.factory.kv_list" .Values.labels | indent 4 }}
{{- end }}